{
  "name": "intro-to-express",
  "version": "1.0.0",
  "description": "### What is Express? Express is a package that we can install with npm. We are going to use it to build our first API. It lets us receive incoming web requests based on their http verb and route. Pop quiz: identify the http verb and route for this request: ``` GET http://your-new-api.com/puppies` ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "rowdy-logger": "^1.0.2"
  }
}
